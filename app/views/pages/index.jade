extends ../layout

block content
	.container
		.row
			- each cat in categories
				.panel.panel-default
					.panel-heading
						h3 
							a(href='/results?cat=#{cat._id}&p=0') #{cat.name}
					.panel-body
						- if (cat.movies && cat.movies.length > 0)
							- for item in cat.movies
								.col-md-2
									.thumbnail
										a(href="/movie/#{item._id}/find")
											- if (item.poster.indexOf('http') > -1)
												img(src="#{item.poster}", alt="#{item.title}")
											- else
												img(src="/upload/movie/#{item.poster}", alt="#{item.title}")
										.caption
											h3 #{item.title}
											p: a.btn.btn-primary(href="/movie/#{item._id}/find", role="button")
												|观看预告片
						- else
							p 
								|	You don't 
								|	hava any
								|	Movies!

		p
			a(href='/admin/movie/new',class='btn primary') Add a Movie
		span #{somesay}
		br
		span #{weathers.weatherinfos[0].temp1}
		|-
		span #{weathers.weatherinfos[0].temp2}
		|&nbsp;&nbsp;&nbsp;
		span #{weathers.weatherinfos[0].weather}

		p
			a(href='/dealersMapData',class='btn primary') get DealersMap
	script.
		//alert('Inline JavaScript in Jade')